import type { InputMode } from '../types/post'

export const MODE_TABS: { value: InputMode; label: string }[] = [
  { value: 'topic', label: 'Thema' },
  { value: 'url', label: 'URL' },
  { value: 'job', label: 'Job' },
]
